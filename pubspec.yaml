name: seed_app
description: Магазин семян — мобильное и веб‑приложение
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase зависимости
  firebase_core: ^2.32.0        # Стабильная версия
  cloud_firestore: ^4.17.5      # Стабильная версия
  firebase_auth: ^4.20.0        # Стабильная версия

  # Аутентификация
  google_sign_in: ^6.1.5        # Стабильная версия
  flutter_web_auth_2: ^3.1.2    # Для Яндекс OAuth

  # UI и утилиты
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  provider: ^6.1.1
  http: ^1.2.0
  cached_network_image: ^3.4.0
  flutter_svg: ^2.0.10
  shared_preferences: ^2.2.2
  collection: ^1.17.2
  go_router: ^14.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/svgs/

  fonts:
    - family: GoogleSans
      fonts:
        - asset: assets/fonts/GoogleSans-Regular.ttf
        - asset: assets/fonts/GoogleSans-Medium.ttf
          weight: 500
        - asset: assets/fonts/GoogleSans-Bold.ttf
          weight: 700